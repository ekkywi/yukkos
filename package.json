{
  "name": "yukkos",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:backend": "npm run start:dev --workspace=apps/backend",
    "start:web": "npm run dev --workspace=apps/web"
  }
}